// credentials.h.example - Rename this file to credentials.h and fill in your WiFi credentials
// This file is used ONLY for development/testing. Once WiFi is configured via UI,
// these defaults are ignored and Preferences storage is used instead.

#ifndef CREDENTIALS_H
#define CREDENTIALS_H

// Optional: Default WiFi credentials for initial setup
// Leave empty ("") to force WiFi configuration via on-screen UI
#define DEFAULT_WIFI_SSID     ""
#define DEFAULT_WIFI_PASSWORD ""

#endif // CREDENTIALS_H
